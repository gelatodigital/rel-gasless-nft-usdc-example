const abi = [
  'function token() view returns (address)',
  'function price() view returns (uint256)',
  'function mint(address to, uint256 amount, uint256 deadline, uint8 v, bytes32 r, bytes32 s)'
];

export default abi;
